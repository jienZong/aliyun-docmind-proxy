# 阿里云文档智能代理服务配置
# 复制此文件为 .env 并修改必要配置

# ===========================================
# 必需配置
# ===========================================
# 服务端口号
PORT=3000

# JWT签名密钥（生产环境必须修改）
JWT_SECRET=your-secret-key-change-in-production

# 允许访问的IP地址（生产环境必须配置）
# 格式：127.0.0.1,::1,192.168.1.0/24
ALLOWED_IPS=127.0.0.1,::1

# ===========================================
# 说明
# ===========================================
# 1. 用户通过API提供自己的AccessKey+Secret
# 2. 服务代替用户调用阿里云SDK
# 3. 返回处理结果给用户
# 4. 无需配置阿里云相关参数（由用户提供）
